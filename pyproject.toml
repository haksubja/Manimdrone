[project]
name = "manimdrone"
version = "0.1.0"
description = "Personal project of a manim animation for the concept of a solar panel cleaning drone."
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "manim>=0.20.1",
]
